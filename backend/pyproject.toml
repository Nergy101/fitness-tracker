[tool.ruff]
# Minimum supported runtime (CI runs 3.12; local venvs may be newer).
target-version = "py312"
exclude = [".venv"]

[tool.ruff.lint]
# Default rule set: E4, E7, E9, F — no E501 (line length), no I (isort)
select = ["E4", "E7", "E9", "F"]
